# CLAUDE.md — TripMind (AI 여행 플래너)

> 이 파일은 새 대화에서 자동 로드되어 프로젝트 맥락을 잡아준다.
> 세부 이력이 필요하면 아래 "관련 세션"을 `search_session_transcripts`로 복원할 것.

## 개요
목적지·날짜·스타일 입력 → AI가 맞춤 일정/예상비용/실시간 날씨·환율 생성하는 여행 플래너.
- **폴더**: `C:\Users\kangj\OneDrive\바탕 화면\TripMind`
- **GitHub**: `Hydrogen-King/TripMind` (private)
- **라이브**: https://tripmind.kangjuno980126.workers.dev/
- **호스팅**: Cloudflare Worker + KV (계정 `kangjuno980126@gmail.com`, `wrangler` 로그인됨)

## 스택
Vanilla HTML/CSS/JS (빌드 도구 없음) · Leaflet.js+OpenStreetMap · Pretendard · html2canvas ·
날씨 wttr.in · 환율 open.er-api.com · PWA(sw.js). 도시 데이터 430개는 `db_extra*.js`에 분산.

## 파일 구조 (핵심)
```
index.html          — 메인 앱(UI + 로직 + 기본 DB)
worker.js           — 메인 Cloudflare Worker
date-proxy-worker.js— 데이트/날짜 프록시 Worker
db_extra*.js        — 추가 도시 DB (없으면 도시 대부분 누락)
hotel_data.js       — 호텔 데이터
sw.js               — Service Worker(오프라인 캐시, BUILD_VERSION으로 무효화)
deploy.sh           — 배포 스크립트(아래)
wrangler.toml / wrangler-date.toml — Worker 설정 2종
SETUP_KV.md, DATE_API_SETUP.md — KV/날짜 API 세팅 문서
```
⚠️ README.md의 Netlify/Vercel 배포 안내는 **구식**. 실제 배포는 아래 Cloudflare 방식.

## 배포 (반드시 이 방식)
`bash deploy.sh` 하나로 메인 Worker + 데이트 프록시 Worker를 배포하고 `BUILD_VERSION`을
타임스탬프로 자동 주입 → SW 캐시 자동 무효화. **수동으로 `wrangler deploy`를 따로 치지 말 것.**
→ `/TripMind:deploy` 스킬이 이 절차를 감싼다.

## 관련 세션 (연결성 있는 개발용)
- `local_7d44f6b1` — 최초 구축(원류)
- `local_e03634cf` — 항공편 예약 기능
- `local_6c834eac` — 아시아나 마일리지 이탈리아/이스탄불 일정(앱 활용)

메모리: `project-tripmind`

---

## 📖 진행 이력 (과거 대화 증류)

**두 개의 모드로 진화:**
1. **여행 계획** — 심리테스트로 취향 파악 → 대륙/국가/도시(다중 선택) 기반 일정.
   목표 도시 수 430개(원래 "5배=410개" 요청에서 시작). 도시별 관광 스팟 5개+ / 대표사진 /
   포토스팟 / 맛집(유튜브·구글평점 우선, 대표메뉴) / 호텔 예시 / 실입장료 기반 예상비용.
   체력 옵션(하루 이동량 조절), 축제·전시 자동반영, 다국가 동시 여행 지원.
2. **데이트 코스** — 한국 한정, 매주용. 출발지별 동선 최적화, 카테고리(한식/중식/일식/양식/
   술집/디저트/브런치 등), Top10 맛집·카페, 영업시간/별점. `tripmind-date` 워커 + `date-proxy-worker.js`.

**항공권**: 결과 화면에 항공권 정보 카드(노선/비행시간/예상 왕복료) + Skyscanner·네이버항공·구글항공
원탭 예약 링크. 추가 API 없이 기존 DB만으로 동작.

**인프라 결정:** 초기엔 Gemini/Groq API 썼으나 무료 한도 문제로 → API 없는 정적 DB 방식으로 전환
(gangnamhouseowner 아키텍처 참고). 배포는 GitHub Pages→Cloudflare Worker로 이전, `deploy.sh`가 표준.
Google Maps는 **Routes API(이동시간)** + **Places API(별점·영업중·사진)** 사용 — 서버키는
`tripmind-date` 워커의 `GOOGLE_KEY` 시크릿, 클라이언트키는 HTML에 리퍼러 제한(도메인 2개)으로 노출.

## 🔧 자주 재발한 이슈 (주의)
- **버튼 클릭 안 됨** — 여러 번 재발. "강건하게 짜달라"는 요청 있었음. 배포 후 항상 버튼 동작 확인할 것.
- 데스크탑 레이아웃에서 글씨 작음/빈 화면 많음 → 반복적으로 "꽉 채우고 글씨 키워달라" 요청.
- 관광지 대표 이미지가 실제와 불일치(같은 이미지 반복) — DB 정확도 이슈.
- 맛집/일정 중복 추천 → 중복 제거 로직 필요.

## ✅ 다음에 이어서 할 일 (미완/열린 항목)
- 추천 장소의 **이유 + 출처(유튜브/네이버 링크)** 표기 — 정윤님 피드백, 일부만 반영.
- 정해진 방문일정(날짜 고정) 반영, 숙소↔관광지 이동시간.
- 아래 "플래닝 교훈" 중 미반영분: 스팟별 휴무일 DB화(요일 매칭 자동 검증), 식사 시간 슬롯 배치.

## 🎓 플래닝 교훈 (2026-07 경주·강릉 가족여행 실전 계획에서 증류)
Claude와 실제 2박3일 가족여행 2건(경주 라한셀렉트, 강릉 탑스텐)을 계획하며 배운 규칙.
✔=코드 반영됨(index.html에 `[교훈]` 주석으로 검색 가능), ☐=미반영.
1. ✔ **마지막 날엔 야경 스팟 금지** — 귀가일에 야경을 넣으면 밤 출발이 됨. `buildItinerary`가 앞날과 자동 교체.
2. ✔ **요일-휴무 충돌 경고** — 월요일은 박물관·미술관·유명 맛집 휴무 잦음(경주 옛정=월 휴무 → 일요일로 조정했던 사례). 날짜별 팁으로 표기.
3. ✔ **체크인 15시 기준 하루 이분법** — 체크인 전엔 숙소 가는 길목 스팟, 체크인·수영장 후엔 숙소 반경 저녁(사용자가 직접 요청했던 수정). 첫날 팁으로 표기.
4. ✔ **귀가일은 집 방향 동선** — 체크아웃 후 스팟은 귀가 경로상 배치(경주 3일차 불국사→시내→IC, 강릉 3일차 정동진→아르떼→미트컬쳐가 실례). 마지막날 팁.
5. ✔ **한여름 야외 스팟은 오전** — 그늘 없는 해안 산책로(파도소리길 등)는 6~8월 한낮 회피. 날짜별 팁.
6. ✔ **결과 화면 플래너 체크리스트** — 예약제 식당(캐치테이블), 브레이크타임 직후 첫 타임 노림수, 밥 간격 3~4h(17시 저녁이면 오후 간식 축소), 성수기 부대시설 요금/패키지 비교, 숙소가 시내 아닐 수 있음(탑스텐=옥계, 묵호가 시내보다 가까웠음).
7. ☐ **스팟·식당 휴무일 DB** — 여행 날짜 요일과 자동 대조해 "이날은 휴무" 검증까지 가면 완성형.
8. ☐ **식사 슬롯 명시 배치** — 아침/점심/저녁을 일정 타임라인에 넣고 간격 검증.

## ✔ 2026-07-09 반영 완료 (전면 리뷰 후속)
- KV 일정 저장 API 첫 정상 배포: `TRIPMIND_KV` 네임스페이스 생성(id `84aa1e07…`),
  wrangler.toml에 `main=worker.js` + `binding="ASSETS"` (ASSETS 바인딩 누락 시 비자산 경로 1101 에러 — 주의).
- `deploy.sh`가 sw.js `SW_VERSION`을 타임스탬프로 sed 주입 → SW 캐시 진짜 자동 무효화.
  index.html에 SW `updatefound` → "새 버전" 토스트 추가.
- 저장/복원: 스냅샷에서 Leaflet 잔재 제거+`maps` 스펙 저장 → 복원 시 지도 재생성(`_initResultMaps`).
  `_lsSet` 실패(쿼터) 시 거짓 성공 토스트 제거.
- 결과 화면 sticky 섹션 내비(`buildResultNav`), 날씨 위젯 여행기간 전체(최대 7일) 표시,
  Routes API sessionStorage 캐시, Leaflet 이중 로드 제거, apple-touch-icon.png 실파일 생성,
  og:url workers.dev로 교체, `.assetsignore`로 소스/설정 공개 서빙 차단, worker.js CORS 화이트리스트.
- netlify.toml/vercel.json 삭제, README Cloudflare 기준 재작성, GitHub Actions CI(test.yml),
  tripmind.test.js 낡은 기대값 수정 + CRLF 정규화 → 133/133 통과.
- `GOOGLE_KEY` 시크릿 등록 완료(리퍼러 제한 없는 서버 전용 키) → `/health`에서 `google:true`,
  데이트 코스 화면에서 실제 별점·리뷰수·영업중 상태 표시 확인됨 (예: "⭐ 4.9 (3,320)").

## 🎓 플래닝 교훈 (2026-09 이탈리아·이스탄불 국제 가족여행 실전 계획에서 증류)
Claude와 로마·피렌체·베네치아·이스탄불 10박11일 가족여행(부모님 동반)을 계획하며 배운 규칙 — 국내여행 교훈의 **국제판**.
✔=코드 반영됨(index.html `[교훈] 해외 여행 체크리스트` 블록 + `_dayLessonTips`, 해외여행 자동판별 `_isOverseas`/`destObj.country`로 검색 가능), ☐=미반영.
1. ✔ **예약 오픈 창(booking window)** — 인기 명소는 방문 N일 전에야 예약이 열리고 금방 매진(바티칸 ~60일·콜로세움 ~30일·보르게세는 창이 더 짧음). 날짜 확정 즉시 명소별 오픈일에 알람. → 해외 체크리스트에 표기.
2. ✔ **공식 사이트 예매(리셀러 회피)** — 구글 검색 상단은 2~3배 비싼 리셀러(GetYourGuide/Tiqets/headout). 공식 도메인 직접. → 체크리스트 + 날짜별 팁(박물관·궁전 키워드 감지 시).
3. ✔ **통합권은 "다 갈 때만" 이득** — 브루넬레스키 패스(5곳 전부·3일 유효)처럼 다 도는 곳이면 이득, 우피치+피티+보볼리 PassePartout처럼 안 갈 곳 끼우면 낭비. 티켓 옵션 선택도 케이스별(산마르코 풀티켓 €36, 두칼레 Secret Itineraries). → 결과화면에 **💳 통합권·패키지 가성비 가이드** 카드로 판단 프레임(살만함/낭비 2열 + 계산법: 실제 갈 곳만 개별합 vs 패키지가) 노출. (index.html `[교훈] 통합권·패키지 가성비 가이드`)
4. ✔ **미술관 피로도 — 하루 대형 2곳까지** — 아카데미아+우피치+피티 3연타는 부모님께 가혹. 중간에 동네(올트라르노) 산책·카페로 리듬 조절. "확실히 다른 것"(돌로미티 대자연 하루)을 끼워 카테고리 분산.
5. ✔ **요일 휴관 정면 대응** — 화요일 휴관(톱카프)·월요일 휴관(바티칸은 일 휴관, 돌마바흐체·보르게세 월 휴관, 그랜드바자르 일 휴무) 흔함. 방문 요일과 대조해 순서 배치. (국내 교훈 #2/☐#7의 국제 강화판)
6. ✔ **도시 이동일 = 오전이동·오후관광** — 이른 점심으로 기차/비행 시간 맞추고, 역·공항 근처 숙소가 짐 이동에 유리(메스트레역 앞 = 돌로미티 투어 픽업지이기도).
7. ✔ **심야 도착 → 공항 인근 1박 후 시내로** — 자정 도착이면 첫 밤은 무료셔틀 공항호텔, 다음날부터 시내 숙소로 옮겨 심야 왕복 제거. 항공 시간대도 일정에서 역산(새벽·심야편은 하루를 통째로 날림).
8. ✔ **식단 제약(햄·돼지) + 아시안 배치** — 무슬림권(터키)은 자동 안전, 이탈리아는 prosciutto·guanciale·pancetta·salame 든 메뉴만 회피(해산물·bistecca·cacio e pepe·피자 OK). 장기 여행엔 아시안 한 끼씩(피렌체 라멘·이스탄불 한식)으로 물림 방지.
9. ✔ **숙소 기준** — 동선·조식포함·환불가능요금·4인 가족룸·심야체크인 확인, 도시세 현장현금. → 체크리스트.
10. ☐ **명소별 "예약 오픈일" DB** — 각 명소의 예약 오픈 시점(방문 N일 전)을 DB화해 "지금 예약 가능/N일 후 오픈" 자동 표기까지 가면 완성형.
11. ☐ **명소별 티켓 옵션·공식 URL DB** — 추천 티켓 등급 + 공식 예매 링크를 스팟 카드에 노출(리셀러 회피 실질화). 국내 교훈의 "출처 링크" 확장.
12. ☐ **식사 슬롯 명시 배치**(국내 교훈 ☐#8과 동일) — 점심/저녁을 타임라인에 넣고 동선 맞춤 식당 + 예약필요 여부 표기. 이번 계획에서 끼니별 식당·예약등급까지 수작업으로 정리했으므로 앱 자동화 여지 큼.

## ✔ 2026-08-08 반영 완료 — 숙소 결정 도우미 6종 (이탈리아 계획 수작업의 자동화)
결과화면 숙소 섹션에 카드 3개 추가. 모두 기존 Routes/Places API만 재사용(새 API 계약 없음).
- **📏 숙소 후보 실측 이동시간 비교** (`_renderAccomCompareCard`/`runAcCompare`) — 후보 × 앵커 3종
  (그날 첫 일정지·교통허브 `CITY_HUBS`·대표명소)를 실측해 표로 비교, 합계 최단 후보 🥇 강조.
  도보/대중교통/차량 모드 토글, 후보 직접 추가(최대 8), **버튼 눌렀을 때만** 호출(비용 보호).
  ⚠️ **이름 문자열 지오코딩은 못 믿는다** — "알레그로 이탈리아노 로마"가 베트남 호이안으로 잡혔던 사례.
  → `_resolvePlace()`로 Places에서 placeId를 먼저 확정(도시 `locationBias` + 60km 초과 결과 폐기)한 뒤
  `_routeWaypoint()`가 `place:<id>` 형식으로 Routes에 넘긴다. 확정된 실주소를 표에 같이 노출.
- **🕒 체크인·체크아웃 충돌 점검** (`_renderCheckinCard`/`evalCheckinConflicts`) — 일정 렌더 중
  `_TRIP_DAYS`에 날짜별 시간대를 모아, 체크인 전/체크아웃 후 일정과 도시이동일 짐 공백을 경고.
  결과 HTML 삽입 **후** 채우는 방식(`#ci-conflict-card` 플레이스홀더) — 일정보다 먼저 렌더되기 때문.
- **🌙 심야 도착 대응** (`runNightArrival`) — 공항→시내를 **현지 심야 시각 기준**으로 실측해 공항 1박 분리 vs 시내 직행 판단.
  ⚠️ **기기 시간대로 계산하면 통째로 틀린다**(로마 자정=한국 오후로 조회됨) → `_destTz()`(도시 영문명 IANA 매칭 →
  `COUNTRY_TZ` 폴백, `TZ_MULTI` 국가는 근사임을 고지) + `_localInstant()`로 절대시각 생성.
  22시 이전 도착은 애초에 "심야 아님"으로 먼저 가른다(안 그러면 낮 도착에도 분리를 권함).
- **🕐 예약 시간대 충돌 검사** (`evalTicketSlots`) — 통합권의 진짜 함정은 가격이 아니라 시간.
  명소별 시간슬롯 DB 없이 사용자가 예약 후보를 입력 → 겹침(⛔)·간격 30분 미만(⚠️) 판정.
  실제 케이스(산마르코 09:30 vs 두칼레 가이드투어 09:00) 재현 확인.
- **⚠️ 같은 브랜드 다른 지점 경고** (`_chainConfusion`) — 첫 토큰이 겹치는 후보가 2곳+면 배지. 후보 추가 시 재계산.
- **⭐ 숙소 평점 읽는 법** — 플랫폼별 기준선 표(에어비앤비 4.5=낮은 편 / 부킹닷컴 9점대=우수) + 리뷰 개수·최신성 안내.
- 부수 유틸: `_josa()`(조사 자동 선택), `_haversineKm()`, `_tzOffsetMin()`. 테스트 133 → **167개 전부 통과**.

## ✔ 2026-08-08 반영 — 🎫 회사 제휴 프로모션 반영 숙소 (개인용 실데이터)
현대차 복지포털에 **등록 완료된** 호텔 체인 프로모션 5종을 `CORP_PROMOS`에 내장(수록 기준 `CORP_PROMO_ASOF`).
- **구조 3종**: `trial`(마리엇·힐튼·하얏트 — 등록 즉시 상위등급 체험) / `achieve`(IHG — 체크아웃 후 등급 확정)
  / `multiplier`(아코르 — 문턱 없이 매 숙박 3배 포인트).
- **판단 순서**: ⓐ 지리적 게이트 → ⓑ 예정 박수 → ⓒ 즉시혜택 환산·문턱 → ⓓ 올해 누적 → ⓔ 실질가 정렬.
  ⓐ가 1순위인 이유: 하얏트가 이스탄불 술탄아흐메트 도보권에 지점이 없어 조건이 통째로 무의미했던 사례.
  구현은 첫 일정지 좌표 기준 Places 검색 → 직선 1.5km 선별 → Routes 도보 실측 → **20분 초과 제외**.
- **체험형 즉시혜택은 문턱 미달이어도 0으로 치지 않는다** — 이번 숙박에 실제로 붙는 가치이므로 환산해 반영.
  반대로 달성형(IHG)은 즉시혜택 0을 명시하고, 문턱 미달 + 올해 추가 계획 없음이면 등급 가치를 0으로 계산.
- **조식·라운지는 등록 성인 2인까지만** 환산 — 인원수만큼 곱하면 4인 가족에서 과대평가된다(초기 버그).
- **동가격대는 승자를 정하지 않는다** — 실질가 차이가 20%(또는 2만원) 이내이면서 도보 5분 이상 차이 나면
  "🚶 위치 우위 / 🎫 프로모션 우위" 2열로 나란히 제시하고 선택은 사용자에게 맡긴다.
- ⚠️ **아코르 회사ID/액세스코드는 소스에 넣지 않음** — index.html이 공개 서빙되므로 대외비 유출.
  `localStorage['tm_corp_codes_v1']`에 기기별 보관. 테스트 `[CORP-05]`가 하드코딩을 막는다.
- 종료일 경과 시 자동 비활성 + D-60 이내면 등록기한 경고, 전량 만료 시 "복지포털 갱신 확인" 배너.
- 테스트 167 → **185개 전부 통과**.

## ✔ 2026-08-08 반영 — 🚉 교통수단 (도시 내 전략 · 도시 간 이동 · 구간 다중수단)
- **`CITY_TRANSIT`** — 도시 30여 곳의 주력 수단·교통권·공항↔시내·렌트 적합도(`no`/`suburb`/`yes`)·특화 팁.
  미수록 도시는 **`COUNTRY_TRANSIT` 국가 폴백**. `_TRANSIT_ALIAS`로 DB 도시명 불일치 해소
  (⚠️ DB는 `kr:'LA'`·`'제주도'`인데 교통DB 키는 `'로스앤젤레스'`·`'제주'` — 별칭 없으면 조용히 국가 폴백으로 떨어짐).
  지역 특화가 핵심: 베네치아 바포레토 1회 €9.50→24h권 손익, 런던 컨택리스(오이스터 불필요),
  이스탄불카트 1장 여러 명 사용, 뮌헨 그룹 1일권 5인, 교토는 버스 도시라 자전거가 최적해 등.
- **`_renderIntercityCard`** — 연속 도시쌍마다 GEO 직선거리 → 밴드별 수단 추천
  (<80 아무거나 / <300 기차 압승 / <700 기차 vs 항공 분기 / 그 이상 항공). 자차 Routes 실측 버튼,
  국가별 `TAXI_KM_KRW`로 택시 추정(도로거리 1.25배 보정) + 인원 분담액, Rome2Rio 범용 비교 링크.
- **렌트 손익분기** (`calcRentBreakeven`) — 렌트는 대수 기준, 기차는 `train1*ppl` 인원수 곱.
  이 비대칭이 핵심이라 4인이면 렌트가 대개 이긴다. 단 ZTL·주차·운전피로는 비용에 안 잡힌다고 명시.
- **구간 다중수단 표기** (`_fetchRealTransitTimes` 개편) — 도보를 먼저 재고 3단계로 분기:
  ≤20분 도보만 / 20~90분 `🚶 35분 · 🚇 22분` 병기 / >90분 대중교통만(도보 시간은 title로).
  ⚠️ 90분 상한이 없으면 "말리부 도보 16시간 38분" 같은 노이즈가 표에 뜬다.
- 테스트 185 → **201개 전부 통과**.

## ✔ 2026-08-08 반영 — 교통 전 도시 커버리지 + 데이트 코스 교통
**450개 도시 전부**가 교통 정보를 받도록 3층 구조로 채웠다. ⚠️ **미수록 도시에 요금·노선명을 지어내지 않는다** —
확인 못 한 "1회 €1.50"은 여행 계획을 망치는 종류의 오류다. 대신 층을 나눠 신뢰도를 명시한다.
1. `CITY_TRANSIT` 33곳 — 검증된 요금·패스·공항편 (배지 없음)
2. `COUNTRY_TRANSIT` **131개국 전부** — 국가 교통 규범·렌트 판단 ("국가 일반 정보" 배지)
3. **좌표 분산 자동 판정** (`_spotSpreadKm`/`_spreadVerdict`) — 그 도시 스팟들의 실제 SPOT_GEO 좌표로
   최대 거리를 계산해 도보권(≤2km)/대중교통(≤8)/택시병행(≤25)/차량필요 판정. 지어내는 게 아니라 계산이다.
   단 SPOT_GEO가 137개뿐이라 이 층이 뜨는 도시는 제한적.
4. `_universalTransitLinks` — Google Maps 대중교통·공항검색. 어느 도시든 최종 안전판.
- ⚠️ 괄호 지역은 본토와 교통이 달라 `미국(하와이)`·`이탈리아(시칠리아)` 등을 **별도 키**로 둔다
  (`_cityTransit`가 괄호 포함 키를 먼저 조회 — 시간대 `COUNTRY_TZ_SUB`와 같은 패턴).
- ⚠️ 전면 재작성 때 **'터키'가 누락**돼 카파도키아·안탈리아 2곳이 무커버였다. `[COV-01]` 테스트가
  DB 국가 목록과 COUNTRY_TRANSIT를 대조해 이 유형의 누락을 자동 검출한다.

**데이트 코스 교통** (`_date_new.js`)
- `KR_REGION_TRANSIT` 권역 13곳 — 주력 수단·교통권·**심야 귀가**(막차/N버스/심야할증 20~40%).
- 구간 실측 `measureDateSegments` — 도보 우선 3단계(≤20분 도보만 / 20~90분 병기 / >90분 대중교통만).
- ⚠️ **데이트 DB 스팟은 실제 장소가 아니라 카테고리 원형**("전시·미술관 관람")이다. 이름으로 경로를 재면
  엉뚱한 값이 나오므로, `_enhanceCourse`가 네이버 실제 장소로 바꾼 슬롯만 잰다. `_applyRealSlot`이
  `dataset.lat/lng`(네이버 좌표)를 심고 `_slotWaypoint`가 `geo:` 웨이포인트로 변환 — 지오코딩보다 정확.
  확정 안 된 슬롯은 추정치를 유지하고 "(추정 · 같은 동네)"로 이유를 밝힌다.
- ⚠️ `_date_new.js`는 **IIFE 캡슐화**라 인라인 onclick 핸들러는 반드시 하단 `Object.assign(window,{...})`에
  추가해야 한다 — 빠뜨리면 버튼이 조용히 죽는다(`[DTR-04]`가 검사).
- ⚠️ 데이트 프록시는 CORS 화이트리스트라 **localhost에서는 실장소 보강이 동작하지 않는다** — 라이브에서 검증할 것.
- 테스트 201 → **213개 전부 통과**.

## ✔ 2026-08-10 반영 — J플래너 벤치마킹 (예약 액션·호출앱·DayPilot 푸시)
유사 앱 j-planner를 보고 얻은 결론: **경쟁이 아니라 단계가 다르다**. TripMind는 결정을 돕고
그쪽은 실행을 돕는데, 우리는 "예약 확정 → 출발 → 현지" 구간이 비어 있었다.
- **✅ 예약 액션** (`BOOKING_WINDOWS`/`_renderBookingActions`) — 명소별 예약 오픈 창(방문 N일 전)을
  계산해 D-day로 노출. 체크리스트에 모아두면 안 하게 되므로 **날짜에 꽂는다**가 핵심.
- **📱 `HAIL_APPS`** — 유럽 다수는 Bolt가 Uber보다 싸고, 동남아 Grab·중동 Careem·중국 DiDi·터키
  BiTaksi처럼 Uber가 없거나 약한 곳이 많다. 앱 이름을 알아야 실제로 부를 수 있다.
- **📅 캘린더 알람** — 기존 `exportToCalendar`에 VALARM이 없었다. 출발 3일 전 + 날짜별 전날 저녁 추가.

### 🔔 DayPilot Web Push 연동 (2026-08-10)
DayPilot에 이미 VAPID Web Push가 있어 푸시를 새로 만들지 않고 **중계**한다.
- TripMind 워커 `/api/reminders`(GET/POST/DELETE) + cron `0 0 * * *`(KST 09시)이 오픈일 도래분을 푸시.
- ⚠️ **토큰을 브라우저에 두면 안 된다** — 누구나 사용자 폰에 푸시 가능. 워커 시크릿 `DAYPILOT_TOKEN`으로만.
- ⚠️ **AGENT_TOKEN을 재사용하지 않았다** — 권한 과다(스크린샷·거래내역)에 값 변경 시 카드 파싱이 깨진다.
  DayPilot에 `TRIPMIND_TOKEN`을 새로 발급하고 `/api/agent/alert` 한 경로만 허용하도록 인증부를 수정.
- ⚠️ **같은 계정 워커를 workers.dev URL로 fetch하면 Cloudflare가 막는다 → `error code: 1042`.**
  반드시 **서비스 바인딩**(`[[services]] binding="DAYPILOT" service="daypilot"` → `env.DAYPILOT.fetch`)
  으로 호출할 것. 이것 때문에 한참 헤맸다. 서버 간 호출이라 DayPilot쪽 CORS도 손댈 필요 없음.
- ⚠️ `/api/notify-test`는 **인증이 없어** 그대로 두면 푸시 스팸 벡터다 → KV로 10분 1회 레이트리밋.
- 시크릿 등록 시 `echo`의 개행이 섞이면 헤더가 깨지므로 워커에서 `.trim()` 방어.

### 🚨 Google은 한국에서 도보·자동차 경로를 제공하지 않는다 (2026-08-10 확인)
지도 데이터 반출 제한 때문에 **국내 좌표쌍은 Routes API WALK/DRIVE가 항상 `null`**이다. TRANSIT만 응답하는데,
70m 거리에도 버스를 태워 **18분**을 돌려준다. 데이트 코스(국내 전용)가 전 구간 "대중교통 15~21분"으로
나온 진짜 원인이 이것이었다 — 장소 해석 문제로 오진하고 가드를 1.5km로 조였다가 서울숲(중심에서 1.6km)이
탈락하는 부작용만 났다. 가드는 3km로 되돌림.
- **해결**: 국내 도보는 좌표로 직접 계산 (`_walkSecsFromCoords`, 4.5km/h × 도로보정 1.3).
  좌표는 실장소면 네이버 mapx/mapy, 아니면 Places 결과의 lat/lng.
- 20분 이하면 **대중교통을 아예 조회하지 않는다** — 부르면 도보보다 느린 값이 나와 오히려 해가 된다.
- 해외는 Routes API 도보가 정상이므로 여행 계획 쪽 로직은 그대로 둔다.
- 활동어 제거(`_cleanSpotQuery`): 데이트 DB 스팟명이 "서울숲 산책·피크닉"처럼 장소명+활동어라
  그대로 검색하면 엉뚱한 곳이 잡힌다 → 활동어를 떼고 검색.
- 결과: 성수동 코스가 도보 4분(232m)·5분(302m)·7분(389m)·12분(708m)·9분(547m)으로 정상화.
